- **Recursion** -  any problem that can be represented from `Nth` order to `N-1th` order
- Recursion done in 3 steps :
    - Convert to loops
    - into functions
    - convert to recursion

### What is Head Recursion in Python?

### What is Tail Recursion in Python?
